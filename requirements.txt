# Core data validation
pandas==2.2.*
numpy==1.26.*

# Great Expectations (file-based, no database required)
great-expectations==0.18.*

# DBT with DuckDB adapter (runs schema + singular tests locally, no database required)
dbt-duckdb==1.9.*

# Testing
pytest==8.3.*
pytest-html==4.1.*
pytest-json-report==1.5.*

# Utilities
tabulate==0.9.*
pyyaml==6.0.*

# SQL demo (file-based, no database server required)
duckdb==1.4.*
